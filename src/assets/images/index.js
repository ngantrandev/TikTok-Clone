import logo from './logo.svg';
import streamIcon from './ic_stream.svg';
import defaultImage from './default-image.png';

const images = {
  logo: logo,
  stream: streamIcon,
  defaultImage: defaultImage,
};

export default images;
